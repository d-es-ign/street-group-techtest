# street-group-techtest

Minimal Expo Router + TypeScript starter scaffolded for interview and tech test use:

- `app/` routes forward to screen modules
- `screens/` owns UI entry screens
- `theme/` owns the shared theme
- `test-utils/` owns shared test helpers
- strict TypeScript, Jest, and ESLint are preconfigured
